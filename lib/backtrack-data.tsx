// 第 5 章 · 回溯 —— 题单与测验数据。
// 题单覆盖 lc.md 回溯模块:组合(77/216/17/39/40)、分割(131/93)、
// 子集(78/90/491)、排列(46/47)、棋盘(51/37),补一道括号生成 22。
// hint 只给方向不剧透,key 用一段话把最优解 + 套路讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 17,
    title: "电话号码的字母组合",
    d: "medium",
    tags: ["组合", "多个集合", "入门"],
    hint: "每个数字对应一组字母,是从「多个不同集合」里各挑一个 —— 树的每一层换一个集合,没有 startIndex。",
    key: (
      <>
        决策树:第 i 层对应第 i 个数字的字母集合,遍历这层的每个字母、加入路径、递归到第 i+1 层、
        再撤销。结束条件是路径长度等于数字个数。它和 77 的区别在于:<b>77 是从同一个集合里选,靠
        startIndex 防重复;17 是每层换一个集合,直接用 index 定位当前该选哪一组</b>,不需要 startIndex。
      </>
    ),
  },
  {
    lc: 77,
    title: "组合",
    d: "medium",
    tags: ["组合", "startIndex", "剪枝", "精讲"],
    hint: "从 1..n 里选 k 个,[1,2] 和 [2,1] 算同一个 —— 怎么保证不重复?再想想:选到一半发现剩下的数不够凑满 k 个,还要往下走吗?",
    key: (
      <>
        dfs(start):路径满 k 个就收集;否则从 start 遍历到 n,选 i、递归 dfs(i+1)、撤销。
        <b>startIndex 保证「只往后选」</b>,自动去掉顺序不同的重复组合。剪枝是本题精髓:
        还需要 <span className="mono">k − path.size</span> 个数,循环上界可以从 n 收紧到
        <span className="mono"> n − (k − path.size) + 1</span> —— 进门前就砍掉注定凑不满的分支。
        本章精讲 A 有剪枝前后两棵决策树的对照动画。
      </>
    ),
  },
  {
    lc: 78,
    title: "子集",
    d: "medium",
    tags: ["子集", "每节点收集", "精讲"],
    hint: "组合是「选够 k 个才算一个答案」,子集呢?想想空集、单元素集也都是答案 —— 收集的时机不一样。",
    key: (
      <>
        和组合同一棵树,唯一区别:<b>不在叶子收,而是每进入一个节点就收一次</b>
        (因为「当前路径」本身就是一个子集,包括空集)。dfs(start):先把当前 path 加入答案,
        再从 start 往后选、递归、撤销。时间 O(n·2ⁿ):2ⁿ 个子集,每个复制一次。
        本章精讲 B 有逐节点收集的动画。
      </>
    ),
  },
  {
    lc: 46,
    title: "全排列",
    d: "medium",
    tags: ["排列", "used 数组", "精讲"],
    hint: "排列讲顺序,[1,2] 和 [2,1] 是两个不同答案 —— 所以不能用 startIndex 只往后选。那怎么知道哪个数已经在路径里了?",
    key: (
      <>
        排列每一层都要能选到「所有还没用过的数」,所以<b>不用 startIndex,改用 used 布尔数组</b>
        记录哪些下标已在路径中。dfs():路径满 n 个就收集;否则遍历每个下标 i,若 used[i] 为真就跳过,
        否则选它(used[i]=true、入路径)、递归、撤销(used[i]=false、出路径)。时间 O(n·n!)。
        本章精讲 C 有 used 数组的逐帧现场。
      </>
    ),
  },
  {
    lc: 216,
    title: "组合总和 III",
    d: "medium",
    tags: ["组合", "双重剪枝"],
    hint: "77 的加强版:不但要选够 k 个,和还必须等于 n。两个约束 = 两处剪枝。",
    key: (
      <>
        在 77 的骨架上加一个 sum:路径满 k 个时,再判断 sum 是否等于 n 才收集。
        两处剪枝叠加:①个数不够(同 77 的上界剪枝);②<b>当前和已经超过 n,后面只会更大,直接停</b>
        (因为 1..9 都是正数)。「多个约束 → 多处剪枝」是组合类进阶题的通用套路。
      </>
    ),
  },
  {
    lc: 39,
    title: "组合总和",
    d: "medium",
    tags: ["组合", "可重复选", "无重复元素"],
    hint: "同一个数可以选很多次 —— 那递归时 startIndex 应该传 i 还是 i+1?",
    key: (
      <>
        元素无重复但<b>每个数可无限次使用</b>:关键改动是递归时传 dfs(i) 而<b>不是 dfs(i+1)</b>,
        让下一层还能再选到自己。start 仍然防止「换个顺序」的重复组合(2+3 和 3+2 只留一个)。
        剪枝:先排序,当 sum + candidates[i] &gt; target 时,后面更大的数也不用试了,直接 break。
      </>
    ),
  },
  {
    lc: 40,
    title: "组合总和 II",
    d: "medium",
    tags: ["组合", "树层去重", "重复元素"],
    hint: "这次数组里有重复元素,但每个元素只能用一次 —— 怎么防止同一层选到两个相同的数,产生重复组合?",
    key: (
      <>
        元素有重复、每个只用一次:先<b>排序</b>,然后<b>树层去重</b> ——
        循环里 <span className="mono">if (i &gt; start &amp;&amp; nums[i] == nums[i-1]) continue;</span>
        跳过「同一层里第二个相同的数」。递归传 dfs(i+1)(每个用一次)。
        本章 §08「去重两板斧」用决策树讲透:为什么是 <span className="mono">i &gt; start</span> 而不是
        <span className="mono"> i &gt; 0</span>。
      </>
    ),
  },
  {
    lc: 90,
    title: "子集 II",
    d: "medium",
    tags: ["子集", "树层去重", "复盘"],
    hint: "78 子集 + 40 的去重。数组有重复元素,怎么让 [1,1] 这样的子集只出现一次、[1] 也不重复出现?",
    key: (
      <>
        78 的每节点收集 + 40 的树层去重的合体:排序后,dfs(start) 里先收 path,
        再用 <span className="mono">i &gt; start &amp;&amp; nums[i] == nums[i-1]</span> 跳过同层重复。
        本章 §08 就是拿 [1,1,2] 的子集树做的对照动画:不去重会冒出两个 {"{1}"}、两个 {"{1,2}"},
        树层去重把那条重复分支整个剪掉。
      </>
    ),
  },
  {
    lc: 47,
    title: "全排列 II",
    d: "medium",
    tags: ["排列", "树层去重", "used"],
    hint: "46 有 used 数组,现在数组还有重复元素。去重条件里那个 !used[i-1],到底在拦什么?",
    key: (
      <>
        排列去重 = 46 的 used 数组 + 树层去重。排序后,跳过条件
        <span className="mono"> i &gt; 0 &amp;&amp; nums[i] == nums[i-1] &amp;&amp; !used[i-1]</span>。
        <b>!used[i-1] 是灵魂</b>:前一个相同的数「已经撤销(不在路径上)」说明是同一层的重复选择,跳过;
        若它还在路径上(used[i-1] 为真),那是「树枝方向」的正常延用,得保留。用 used[i-1]（树枝去重）
        也能得到正确答案,但剪得晚、更慢 —— §08 讲清两者区别。
      </>
    ),
  },
  {
    lc: 131,
    title: "分割回文串",
    d: "medium",
    tags: ["分割", "startIndex 当切割线"],
    hint: "把「切割」也看成决策:startIndex 不再是选哪个数,而是「下一刀切在哪」。",
    key: (
      <>
        分割问题的心法:<b>startIndex 表示「上一刀之后、下一段的起点」</b>。dfs(start):从 start 到末尾
        枚举切点 i,取子串 s[start..i];若它是回文,就加入路径、递归 dfs(i+1)、撤销;不是回文则跳过
        (这就是剪枝)。start 到达串尾 = 找到一种完整切法。「切割 = 在字符间隙做选择」是分割类的统一模型。
      </>
    ),
  },
  {
    lc: 93,
    title: "复原 IP 地址",
    d: "medium",
    tags: ["分割", "多重剪枝"],
    hint: "还是切割,但限制多:必须切成 4 段,每段 0~255,且不能有前导零。约束越多,剪枝越狠。",
    key: (
      <>
        131 的切割框架 + 一堆合法性剪枝:每段长度 1~3、数值 ≤ 255、除「0」本身外不能有前导零;
        已经切了 4 段就必须正好用完所有字符。<b>把「段数」当第二个维度</b>(切满 4 段是结束条件),
        每段的合法性检查失败就直接剪掉。它和 131 是同一套分割骨架,区别只在剪枝规则的多少。
      </>
    ),
  },
  {
    lc: 491,
    title: "递增子序列",
    d: "medium",
    tags: ["子集", "去重", "不能排序"],
    hint: "求所有递增子序列,又要去重 —— 但这次不能先排序(一排序就破坏了原有顺序)。那树层去重靠什么?",
    key: (
      <>
        坑点:要保持原顺序,<b>不能排序</b>,所以 40/90 的「和前一个比」失效。改用<b>本层的 set</b>:
        在每一层 dfs 内部开一个 used/HashSet,记录「这一层已经选过哪些值」,同层遇到重复值就跳过。
        再叠加「必须递增(nums[i] ≥ path 末尾)」的条件。它是「树层去重」在不能排序时的变体 ——
        本质仍是「同一层不选两个相同的值」。
      </>
    ),
  },
  {
    lc: 22,
    title: "括号生成",
    d: "medium",
    tags: ["组合", "合法性剪枝", "补充"],
    hint: "每一步只有两个选择:放「(」或放「)」。但什么时候放「)」是非法的?想清楚就是最好的剪枝。",
    key: (
      <>
        决策树每层二选一。剪枝规则:<b>左括号用了不到 n 个才能放「(」;右括号数量小于左括号数量才能放「)」</b>
        (否则会出现「)」在前的非法串)。左右都用满 n 个 = 一个合法解。这道题几乎没有「回溯的撤销」感
        (常用参数传递代替显式撤销),但它是「用约束当剪枝」的绝佳范例。
      </>
    ),
  },
  {
    lc: 51,
    title: "N 皇后",
    d: "hard",
    tags: ["棋盘", "冲突检测", "精讲"],
    hint: "逐行放皇后,每行选一列。放下前先问:这一列、两条斜线上有没有已经放好的皇后?",
    key: (
      <>
        把「行」当决策树的层:dfs(row) 遍历每一列 col,若 (row, col) 不被上方任何皇后攻击
        (同列、同主对角线 row−col、同副对角线 row+col),就放下、递归 dfs(row+1)、撤销。
        row 到达 n = 找到一个解。冲突检测可以用三个布尔数组(列 / 两对角线)做到 O(1)。
        本章精讲 D 有 4×4 棋盘的逐行放置 + 冲突 + 回退动画。
      </>
    ),
  },
  {
    lc: 37,
    title: "解数独",
    d: "hard",
    tags: ["棋盘", "二维回溯", "位运算进阶"],
    hint: "找到第一个空格,试填 1~9,填一个合法数字就递归解剩下的;填不下去就退回来换一个。",
    key: (
      <>
        二维棋盘上的回溯:找一个空格,枚举 1~9,若填入不违反行/列/宫,就填下、递归求解;
        递归成功则一路返回 true,失败就擦掉重填(撤销)。剪枝靠合法性检查。<b>进阶版用位运算
        把每行/每列/每宫的「已用数字」压成一个 int,判重和取候选都变成位操作(见第 04 章位运算)</b> ——
        这正是 lc.md 规则 2 点名的「同题两解:普通回溯 + 位运算优化」。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: "回溯算法的本质,最准确的一句话是?",
    opts: [
      "在一棵「决策树」上做深度优先搜索(DFS):每一步做一个选择,走到底或走不通就撤销选择、换下一个",
      "一种比 DP 更快的动态规划",
      "把所有情况用多重 for 循环枚举出来",
      "一种专门用来排序的分治算法",
    ],
    correct: 0,
    wrong: [
      undefined,
      "恰恰相反:回溯是「不重不漏地穷举」,通常是指数级复杂度;DP 之所以快,正是因为它记住了重叠子问题,回溯没有这种记账。",
      "多重 for 循环只能处理「层数固定」的枚举。回溯的强项是层数不定(选几个数、切几刀、放几个皇后由搜索过程决定)—— 这是 for 循环写不出来的。",
      "排序是排序,回溯是穷举搜索。回溯确实是「递归 + 分支」,但它不做分治合并,而是枚举所有决策路径。",
    ],
    why: "把问题组织成决策树,用 DFS 遍历每条路径,走进死胡同就回退换路 —— 这就是回溯。本章所有题目的差别,只在于「树长什么样」和「怎么剪枝」。",
  },
  {
    type: "choice",
    q: "LC 77 组合:从 1..n 里选 k 个数。为什么递归时要传一个 startIndex,只从它往后选?",
    opts: [
      "因为组合不讲顺序,[1,2] 和 [2,1] 是同一个 —— 只往后选能保证每个组合按升序生成,天然去重",
      "为了让代码跑得更快,和正确性无关",
      "因为不传 startIndex 会导致数组越界",
      "因为 startIndex 是排列问题的标志",
    ],
    correct: 0,
    wrong: [
      undefined,
      "它首先是<b>正确性</b>问题,不只是速度:不加 startIndex,每一层都能选到所有数,就会同时产生 [1,2] 和 [2,1],组合被重复计数。",
      "越界和 startIndex 无关。不传 startIndex 不会越界,只会产生大量重复的组合。",
      "正好说反了:startIndex 是<b>组合</b>的标志(只往后选);<b>排列</b>讲顺序,反而不能用 startIndex,要用 used 数组。",
    ],
    why: "组合无序,用 startIndex 强制「只往比当前大的方向选」,让每个组合只以升序出现一次 —— 这是组合类去重的根本手段。",
  },
  {
    type: "fill",
    q: <>LC 77:n = 4、k = 2,一共有多少个组合?(在决策树上数叶子,或用 C(4,2))</>,
    placeholder: "输入一个整数…",
    answers: ["6", "6个"],
    hint: "C(4,2) = 4×3 / (2×1)。也可以直接数:[1,2] [1,3] [1,4] [2,3] [2,4] [3,4]。",
    why: "C(4,2) = 6,正是本章精讲 A 决策树里的 6 个绿色叶子。而「选 4 起头」那一支凑不满 2 个,被剪枝提前砍掉。",
  },
  {
    type: "choice",
    q: "组合、子集、排列都在同一类决策树上跑。它们「收集答案」的时机分别是?",
    opts: [
      "组合/排列在叶子(路径满足长度要求)收;子集在每一个节点都收(路径本身就是一个子集)",
      "三者都只在叶子收集",
      "三者都在每个节点收集",
      "组合在每个节点收,子集和排列在叶子收",
    ],
    correct: 0,
    wrong: [
      undefined,
      "子集不是只在叶子收:空集、{1}、{1,2} 都是答案,它们分别出现在根、第一层、第二层 —— 所以子集要在<b>每个节点</b>收。",
      "组合和排列有明确的长度/完整性要求(选够 k 个、排满 n 个),只有到达叶子才算一个答案,不能在中途节点收。",
      "正好搞反了。组合要凑够 k 个才是一个答案(叶子收);子集是「走到哪收到哪」(每节点收)。",
    ],
    why: "记住这句话:「路径本身有意义 → 每个节点收(子集);路径要满足条件才算数 → 叶子收(组合/排列)」。收集时机是这三类题最容易混的点。",
  },
  {
    type: "choice",
    q: "LC 46 全排列为什么用 used 布尔数组,而不像组合那样用 startIndex?",
    opts: [
      "排列讲顺序,每一层都要能选到「所有还没用过的数」(包括比当前小的),startIndex 会漏掉它们;used 只排除「已在路径中」的数",
      "used 数组比 startIndex 更省内存",
      "因为排列不需要去重",
      "两者随便用哪个都行,习惯问题",
    ],
    correct: 0,
    wrong: [
      undefined,
      "内存不是重点(used 反而多花一个数组)。关键是<b>能不能选到该选的数</b>:startIndex 只让你往后选,而排列需要回头选前面的数。",
      "全排列(46)本身元素不重复,确实不用去重;但这题的核心不是去重,是「用 startIndex 会漏掉答案」—— 比如 [2,1] 里的 1 在 2 后面,startIndex 就选不到了。",
      "不是习惯问题:排列用 startIndex 会直接漏解([2,1]、[3,1] 都出不来)。这是正确性差异,不是风格差异。",
    ],
    why: "组合「只往后选」用 startIndex;排列「顺序不同算不同答案」,每层都要能选全部未用过的数,所以必须用 used 记录占用情况。",
  },
  {
    type: "choice",
    q: "LC 47 全排列 II(数组含重复)的去重条件是 i>0 && nums[i]==nums[i-1] && !used[i-1]。这里的 !used[i-1] 在拦什么?",
    opts: [
      "拦「同一层」的重复:前一个相同的数刚被撤销(不在路径上),说明本层已经拿它开过一模一样的分支了,当前这个跳过 —— 这就是树层去重",
      "拦数组越界,防止 i-1 变成负数",
      "拦「路径太长」的情况",
      "它没有作用,删掉结果也一样",
    ],
    correct: 0,
    wrong: [
      undefined,
      "越界由前面的 i>0 负责。!used[i-1] 判的是「前一个相同元素此刻在不在路径里」,和越界无关。",
      "它和路径长度无关。used[i-1] 描述的是「下标 i-1 的数当前用没用」,是去重的方向判断。",
      "删掉就会产生大量重复排列。!used[i-1] 是区分「树层重复(要剪)」和「树枝延用(要留)」的开关,是去重的核心。",
    ],
    why: "!used[i-1] 为真 = 前一个相同的数已在本层用过并撤销 = 同层重复 → 剪掉(树层去重)。若 used[i-1] 为真,是路径上的正常延用,必须保留。用 used[i-1](树枝去重)也能得对,但剪得晚、更慢。",
  },
  {
    type: "multi",
    q: "关于「剪枝(pruning)」,下列哪些说法正确?(多选)",
    opts: [
      "剪枝的本质是:在进入一棵子树之前,先判断「这条路还有没有可能成功」,没可能就不进",
      "LC 77 选 k 个数时,若剩下的数已经不够凑满 k 个,这一支可以直接不进(个数剪枝)",
      "剪枝能改变问题的答案,让结果变少",
      "剪枝只是常数优化,对指数级搜索毫无意义",
    ],
    correct: [0, 1],
    missHint: "剪枝的两大典型:①「进门前判断可行性」的通用思想;②77 的个数上界剪枝。你漏了其中一个。",
    extraHint: "剪枝<b>不改变答案</b>、也<b>绝非无用</b> —— 你多选了一个错误项。剪掉的都是「注定失败」的分支,答案一个不少,但速度可能快几个数量级。",
    why: "剪枝 = 提前否定不可能成功的分支,答案不变、搜索量骤减。77 的上界从 n 收紧到 n−(k−已选)+1 就是典型;组合总和里「和已超标就 break」也是。",
  },
  {
    type: "choice",
    q: "回溯模板里,每次递归返回后都要「撤销选择」(path.pop()、used[i]=false、擦掉棋盘)。如果忘了撤销会怎样?",
    opts: [
      "路径状态会被上一条分支「污染」,下一个选择是在错误的现场上做的,结果全错",
      "只是慢一点,结果仍然正确",
      "会导致栈溢出",
      "没有任何影响,撤销是可有可无的",
    ],
    correct: 0,
    wrong: [
      undefined,
      "不是快慢问题,是对错问题:不撤销,path 里会残留上一条路径选过的东西,兄弟分支基于脏数据继续,答案直接错。",
      "栈溢出是递归不收敛(没有终止条件)才会发生。忘记撤销不影响递归深度,只会弄脏共享的路径状态。",
      "撤销是回溯的「回」字所在。忘了它,决策树的兄弟分支就共用了被污染的路径,几乎必错。",
    ],
    why: "「加入 → 递归 → 撤销」是回溯的三部曲,撤销负责把现场恢复成进入这个节点之前的样子,好让兄弟分支从干净状态出发。少了撤销,回溯就名存实亡。",
  },
];
